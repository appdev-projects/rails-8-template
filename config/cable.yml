# Async adapter only works within the same process, so for manually triggering cable updates from a console,
# and seeing results in the browser, you must do so from the web console (running inside the dev process),
# not a terminal started via bin/rails console! Add "console" to any action or any ERB template view
# to make the web console appear.
development:
  adapter: async

test:
  adapter: test

# Solid Cable polls the same metered external database as Solid Queue (see
# config/queue.yml), but only while someone is subscribed, so this costs nothing
# until an app adds turbo_stream_from. A second is still "instant" for a Turbo
# broadcast; lower SOLID_CABLE_POLL_INTERVAL for typing indicators or live cursors.
production:
  adapter: solid_cable
  polling_interval: <%= ENV.fetch("SOLID_CABLE_POLL_INTERVAL", 1) %>.seconds
  message_retention: 1.day
